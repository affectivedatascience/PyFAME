# Timing

Provides timing functions used together with a `TimingConfiguration` object to precisely modulate within video manipulation onsets, offsets and strength curves.

::: pyfame.layer._layer
    options:
      members:
        - TimingConfiguration

---

::: pyfame.layer.timing_curves